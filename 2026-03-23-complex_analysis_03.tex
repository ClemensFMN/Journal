\DiaryEntry{Complex Analysis, 3}{2026-03-23}{Complex Analysis}

In this entry we consider mappings between complex functions (in a similar spirit as we considered the mappings induced by the complex exponential in the previous entry).


\subsection{Complex Factor}

The most simple case is multiplication by a complex factor $a$; ie

\bee
w = a \cdot z
\eee

We can see the effect of the mapping more clearly by considering polar coordinates. With $z = r e^{j \theta}$ and  $a = |a| e^{j \alpha}$, we obtain

\bee
w = |a| \cdot z = a r e^{j (\theta + \alpha)}
\eee

Tt follows that the transformation expands or contracts the vector representing $z$ by the factor $|a|$ and rotates it through an angle $\alpha$ about the origin. The image of a given region is therefore geometrically similar to that region. Thus, in particular, a dilation maps a straight line onto a straight line and a circle onto a circle.


\subsection{Linear Mapping}

A bit more complex is the general linear mapping given by

\bee
w = a \cdot z + b
\eee

with $a = |a| e^{j \alpha}$, $b = b_x + j b_y$. This corresponds to a expansion or contraction (easiest represented in polar coordinates) followed by a translation by $b$ (easiest represented in rectangular coordinates).

\subsection{Exponentiation}

\bee
w = z^n
\eee

With $z = r e^{j \theta}$, we obtain $w = r^n e^{j n \theta}$. Below plot shows the effect on lines: Lines on the real and imaginary axis in the $z$-plane are mapped to lines in the $w$-plane; all other lines are mapped to curves.

For example, the curve $x = x, y = t$ gives $z = x + jt \rightarrow w = z^2 = x^2 - t^2 + j 2xt$. This corresponds to $u = x^2 - t^2, v = 2xt$. We can arrange this to $t = v / 2x$ and then we obtain

\bee
u = x^2 - t^2 = x^2 - \frac{v^2}{4x^2}
\eee

which are parabolas. 

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-complex_01.png}
\end{figure}

It's also interestng to note that circle stays circles as shown in the plot below. We best see that in the polar expression, $w = r^n e^{j n \theta}$: The radius changes and the circle is traversed two times (so a half-circle becomes a full circle as shown below).

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-complex_02.png}
\end{figure}


\subsection{Mapping $1/z$}

This mapping can be rewritten according to

\be\label{2026-03-23:eq1}
w = \frac{1}{z} = \frac{\bar{z}}{|z|^2}
\ee

This is a reflection on the real axis (due to the conjugate operation) and a scaling by $1 / |z|^2$. So points outside the unit circle will be moved inside the unit circle and vice versa. We can also see that by using polar coordinates, we then have

\bee
w = \frac{1}{z} = \frac{1}{re{j \phi}} = \frac{1}{r} e^{- j \phi}
\eee

Lines on the real and imaginary axis are mapped into lines again. This is shown in the plot below.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-complex_10.png}
\end{figure}

We start with a line on the real axis, $x = t, y = 0, z = t$. In this case the mapping yields $w = 1/t$. A line on the imaginary axis is given by $z = jt$, the mapping yields $w = -j / t$. \qed


Arbitrary lines are mapped onto circles as shown in the following plot.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-complex_11.png}
\end{figure}

And finally, circles are mapped onto circles; interestingly, not only circles with center at the origin, but circles with \emph{any} center point are mapped into circles.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-complex_12.png}
\end{figure}

\paragraph{Analysis of Mapping of Circles.} To see the effect of the mapping, we start with equation of a circle with radius $r$ and center point $c$,

\bee
|z - c| = r
\eee

Substituing $z = 1/w$, we obtain

\bee
\left| \frac{1}{w} - c \right| = r \rightarrow |\frac{1 - wc}{w}| = r \rightarrow |1 - wc| = r |w|
\eee

We can square both sides; expanding the RHS yields

\bee
(1 - wc)(1 + \overline{wc}) = r^2 |w|^2 \rightarrow 1 - wc - \overline{wc} + |c|^2 |w|^2 = r^2 |w|^2 \rightarrow 1 - wc - \overline{wc} + |w|^2 (|c|^2 - r^2) = 0
\eee

We rearrange terms

\bee
|w|^2 - \frac{wc + \overline{wc}}{|c|^2 - r^2} + \frac{1}{|c|^2 - r^2} = 0 \rightarrow |w|^2 - \frac{wc + \overline{wc}}{|c|^2 - r^2} = - \frac{1}{|c|^2 - r^2} 
\eee

We now complete the square to obain a quadratic expression in $w$,

\bee
\left| w - \frac{\overline{c}}{|c|^2 - r^2}\right|^2 = \frac{|c|^2}{(|c|^2 - r^2)^2} - \frac{1}{|c|^2 - r^2} = \frac{|c|^2 - |c|^2 + r^2}{(|c|^2 - r^2)^2} = \frac{r^2}{(|c|^2 - r^2)^2}
\eee

From this we see that this is a circle with center point 

\be\label{2026-03-23:eq5}
\frac{\overline{c}}{|c|^2 - r^2}
\ee

and radius 

\bee
\frac{r}{|c|^2 - r^2}
\eee

As stated above, the circle in the $z$-plane is mapped onto a circle in the $w$-plane; however, the circle in the $w$-plane has a center given by \eqref{2026-03-23:eq5} (which depends on both $c$ and $r$). The center $c$ of the circle in the $z$-plane is mapped onto $1 / c = \frac{c_x - jc_y}{c_x^2 + c_y^2}$, but this is a \emph{different} point than \eqref{2026-03-23:eq5}.

Only in the limit of small circles, $r \rightarrow 0$, the center point \eqref{2026-03-23:eq5} becomes $\bar{c} / |c|^2 = 1 / c$. \qed

The mapping transforms circles with center in the origin, $c = 0$, into circle with center point in the origin and radius $1/r$.



\subsection{Moebius Transform}

The Moebius transform is given by

\bee
w = \frac{az + b}{cz + d}
\eee

and is the combination of three mappings: A linear mapping $w_1 = az+b$, an inversion $w_2 = 1/w_1$, and a final linear mapping $w = \frac{a}{c} + \frac{bc - ad}{c} w_2$. The last transform may not be obvious, but we can show that it all fits together,

\begin{align*}
    w &= \frac{a}{c} + \frac{bc - ad}{c} w_2 = \frac{a}{c} + \frac{bc - ad}{c} \frac{1}{w_1} = \frac{a}{c} + \frac{bc - ad}{c} \frac{1}{cz+d} \\
    &= \frac{a(cz+d) + bc - ad}{c(cz+d)} = \frac{acz+ad + bc - ad}{c(cz+d)} = \frac{acz+ bc}{c(cz+d)} = \frac{az + b}{cz + d} \qed
\end{align*}

From this and the linear mapping discussed above, we see that the Moebius transform will transform circles into circles (considering lines as special circles with infinite radius).

There is always a linear fractional transformation that maps three given distinct points $z_1 , z_2, z_3$ onto three specified distinct points $w_1, w_2, w_3$, respectively. This transform is given by

\begin{equation}\label{2026-03-24:eq6}
    \frac{(w - w_1)(w_2 - w_3)}{(w - w_3)(w_2 - w_1)} = \frac{(z - z_1)(z_2 - z_3)}{(z - z_3)(z_2 - z_1)}
\end{equation}

We can see this by rewriting this as

\begin{equation*}
    (z - z_3)(z_2 - z_1)(w - w_1)(w_2 - w_3) = (z - z_1)(z_2 - z_3)(w - w_3)(w_2 - w_1)
\end{equation*}

If $z = z_1$, the RHS becomes zero, and for the LHS to become zero as well, we need $w = w_1$. In a similar spirit, $z = z_3$ zeros the LHS, and $w = w_3$ does that for the RHS. For the third case we set $z = z_2$ and this cancels the $z_2 - z_3$ term and the $z_2 - z_1$ on both sides. We are left with

\begin{equation*}
    (w - w_1)(w_2 - w_3) = (w - w_3)(w_2 - w_1)
\end{equation*}

and from this follows $w = w_2$. \qed

So having three points $z_1 , z_2, z_3$ and $w_1, w_2, w_3$, we can insert into \eqref{2026-03-24:eq6} to obtain a suitable Moebius transform.

\paragraph{Example.} For fun, let's chose $z_1 = 1, z_2 = j, z_3 = 2$ and $w_1 = 2, w_2 = -j, w_3 = 4$. Inserting into \eqref{2026-03-24:eq6} yields

\begin{equation*}
    \frac{(w - 2)(-j - 4)}{(w - 4)(-j - 2)} = \frac{(z - 1)(j - 2)}{(z - 2)(j - 1)}
\end{equation*}

Sticking this into Maxima, we can solve for $w$ to obtain

\begin{verbatim}
(%i1)	lhs:(w-2)*(-%i-4)/(w-4)/(-%i-2);
(%o1)	((-%i-4)*(w-2))/((-%i-2)*(w-4))
(%i3)	rhs:(z-1)*(%i-2)/(z-2)/(%i-1);
(%o3)	((%i-2)*(z-1))/((%i-1)*(z-2))
(%i6)	sol:solve(rhs=lhs, w);
(%o6)	[w=((6*%i+10)*z-12*%i)/(3*%i*z-6*%i+5)]
\end{verbatim}

We can check the result; eg for the point $z = 2$

\begin{Verbatim}
(%i11)	sol_cal:rhs(sol[1]);
(%o11)	((6*%i+10)*z-12*%i)/(3*%i*z-6*%i+5)
(%i30)	rectform(subst(z=2, sol_cal));
(%o30)	4
\end{Verbatim}

We can use a python script to plot the transform for several circles in the $z$-plane as shown below. Circles with center point at the origin are not transformed into circles at the origin; this is different from the (simpler) $1/z$ mapping.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-moebius_01.png}
\end{figure}

\paragraph{Example.} Maybe above example was not so helpful as we only defined three points and not a circle and therefore we could not check the effect on the circle. Let's try again with $z_1 = -1, z_2 = j, z_3 = 1$ which is a circle through the origin and radius $1$. The three points shall be mapped onto $w_1 = -1, w_2 = 3, w_3 = 4+2j$. Inserting into \eqref{2026-03-24:eq6} and solving for $w$, we obtain the Moebius transform

\bee
w = \frac{(-23+5j)*z-25 + 11j}{(-5+7j)z - 3 +j}
\eee

Defining this mpaping in python

\begin{verbatim}
    def mpg(z):
    return((5*1j-23)*z - 25+11*1j)/((7*1j-5)*z - 3+1j)
\end{verbatim}

we can check that the mapping works as intended,

\begin{verbatim}
    mpg(-1)
    Out[5]: (-1-0j)

    mpg(1j)
    Out[6]: (3-0j)

    mpg(1)
    Out[7]: (4+2j)
\end{verbatim}


The plots of the circle in the $z$-plane and in the $w$-plane is shown below.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-03-23-moebius_02.png}
\end{figure}




\paragraph{Different Calculation for $w = 1/z$ - not finished}

We can make the effect of the mapping more clear by expanding \eqref{2026-03-23:eq1},

\bee
w = u + jv = \frac{1}{z} = \frac{\bar{z}}{|z|^2} = \frac{x - jy}{x^2 + y^2}
\eee

From this we obtain,

\be\label{2026-03-23:eq2}
u = \frac{x}{x^2 + y^2} = \frac{x}{r}, \quad v = \frac{-y}{x^2 + y^2} = - \frac{y}{r}
\ee

with $r = x^2 + y^2$. However, this $r$ is \emph{totally} unrelated to the circle radius $r$ defined below.

We can represent a general circle with center $(c_x, c_y)$ and radius $r$ as

\bee
(x - c_x)^2 + (y - c_y)^2 = r^2
\eee

Expanding this, we obtain

\bee
x^2 - 2x c_x + c_x^2 + y^2 - 2 y c_y + c_y^2 - r^2 = 0 \rightarrow x^2 + y^2 - 2c_x x - 2c_y y + c_x^2 + c_y^2 - r^2 = 0
\eee

When we compare this with the general expression

\bee
A(x^2 + y^2) + Bx + Cy + D = 0
\eee

we see that

\bee
A = 1, \quad B = -2c_x, \quad C = -2c_y, \quad D = c_x^2 + c_y^2 - r^2
\eee



Now we investigate the effect of the complex function: We rewrite \eqref{2026-03-23:eq2} as

\bee
x = ur, \quad y = -vr
\eee

Substituing these back into $r = x^2 + y^2$ yields $r = r^2 (u^2 + v^2) \rightarrow r = \frac{1}{u^2 + v^2}$. Now we substitute back into the original circle equation,

\begin{align*}
A(x^2 + y^2) + Bx + Cy + D &= A r + B ur - C vr  + D \\ 
&= A \frac{1}{u^2 + v^2} + B \frac{u}{u^2 + v^2} +C \frac{-v}{u^2 + v^2} + D = 0    
\end{align*}

Multiplying with $u^2 + v^2$ yields

\bee
D (u^2 + v^2) + B u - C v + A = 0
\eee

and dividing by $D$ we finally obtain

\bee
u^2 + v^2 + \frac{B}{D} u - \frac{C}{D} v + \frac{A}{D} = 0
\eee

and this is again a circle equation. But I'm not sure how we can obtain the cente rpoint and radius of this circle in the $u-v-$plane in terms of $c$ and $r$ like we did in the previous subsection.




%%% Local Variables:
%%% mode: latex
%%% TeX-master: "journal"
%%% End:
