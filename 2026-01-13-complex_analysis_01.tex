\DiaryEntry{Complex Analysis, 1}{2026-01-13}{Complex Analysis}

\subsection{Introduction}

Based on \cite{agarwal2011complanalysis}. We consider a complex number $z = x + jy$ and have $\Re z = x, \Im z = y$. The complex conjugate $\bar{z}$ is defined as $\bar{z} = x - jy$ and from this we obtain the following

\begin{equation*}
\Re z = \frac{z + \bar{z}}{2}, \Im z = \frac{z - \bar{z}}{2j}        
\end{equation*}


There is the triangle inequality

\begin{equation*}
|z_1 + z_2| \leq |z_1| + |z_2|    
\end{equation*}

When we apply the inequality to $z_2 - z_1$ and $z_1$, we get another interesting form

\begin{equation*}
|z_2| \leq |z_2 - z_1| + |z_1| \rightarrow |z_2| - |z_1| \leq |z_2 - z_1|    
\end{equation*}

We can use the triangle inequality in the following bound

\begin{align*}
    |z_1 + z_2|^2 &= (z_1 + z_2)(\bar{z_1} + \bar{z_2}) = z_1 \bar{z_1} + z_1 \bar{z_2} + z_2 \bar{z_1} + z_2 \bar{z_2} \\
    &= |z_1|^2 + (z_1 \bar{z_2} + \overline{z_1 \bar{z_2}} )+ |z_2|^2 \\
    &= |z_1|^2 + 2 \Re (z_1 \bar{z_2}) + |z_2|^2 \\
    &\leq |z_1|^2 + 2 |z_1 z_2| + |z_2|^2 = (|z_1| + |z_2|)^2
\end{align*}

\paragraph{Examples.} When $|z| = 1$, we can bound

\begin{equation*}
|z^2 + 2z + 6 + 8j| \leq |z|^2 + 2 |z| + |6 + 8j| = 1 + 2 + \sqrt{36 + 64} = 13 \qed
\end{equation*}


We can use the polar form of a complex number $e^{j \theta} = cos \theta + j \sin \theta$ in the following

\begin{equation*}
    (\cos \theta + j \sin \theta)^n = \left( e^{j \theta} \right)^n = e^{j n \theta} = \cos n \theta+  j \sin n \theta
\end{equation*}

As a concrete example, consider $n=3$. Then we have

\begin{align*}
    \cos 3 \theta + j \sin \theta &= e^{j3 \theta} = \left(e^{j \theta}\right)^3 = (cos \theta + j \sin \theta)^3 \\
    &= \cos^3 \theta + 3j\cos^2 \theta \sin \theta - 3 \cos \theta \sin^2 \theta - j \cos^3 \theta
\end{align*}

By comparing real and imaginary parts we finally arrive at

\begin{align*}
    \cos 3 \theta &= \cos^3 \theta +  - 3 \cos \theta \sin^2 \theta \\
    \sin 3 \theta &= 3\cos^2 \theta \sin \theta - \cos^3 \theta
\end{align*}

We also have

\bee
(1 + j)^{24} = \left( \sqrt{2} e^{j \pi / 4} \right)^{24} = \sqrt{2}^{24} e^{j 24 \pi / 4} = 2^{12} e^{j 6 \pi} = 2^{12} \qed
\eee


\subsection{Complex Exponential}

The exponential function is defined as below,

\be\label{2026-02-05:eq1}
e^{z} = e^{x + jy} = e^x e^{jy} = e^x (\cos(y) + j \sin(y))
\ee

Let's consider some properties of the complex exponential. We have

\bee
e^z = 1 \, \text{iff} \, z = 2k\pi j
\eee

for any integer $k$. Using $z = x + jy$ we have $|e^z| = |e^x| |e^{jy}| = e^x = 1$ and therefore $x = 0$. So we have $e^z = e^{jy} = \cos y + j \sin y$. Equating real and imaginary parts, we have $\cos y = 1, \sin y = 0$ and this is satisfied iff $y = 2k\pi$ with $k$ being some integer.
In a similar spirit, we have

\bee
e^{z_1} = e^{z_2} \, \text{iff} \, z_1 = z_2 + 2k\pi j
\eee

Since, for all $z$, $e^{z+2k\pi j} = e^z$, we find that $e^z$ is periodic with complex period $2\pi j$. Consequently, if we divide the $z$-plane into the infinite horizontal strips

\bee
S_n = \{ x + jy: -\infty < x < \infty, \, (2n - 1) \pi < y \leq (2n+1) \pi, \, n = 0, \pm 1, \pm 2, \ldots \}
\eee

then $e^z$ will behave in the same manner on each strip. We also see that $e^z$ is one-to-one on each sttrip $S_n$.

We can insert \eqref{2026-02-05:eq1} into the Cauchy-Riemann equations to check whether the complex exponential is analytic,

\bee
\frac{\partial u}{\partial x} = \frac{\partial v}{\partial y}, \quad \frac{\partial u}{\partial y} = - \frac{\partial v}{\partial x}
\eee

We have

\begin{align*}
    \frac{\partial u}{\partial x} &= e^x \cos y, \quad \frac{\partial v}{\partial y} = e^x \cos y \\
    \frac{\partial u}{\partial y} &= - e^x \sin y, \quad \frac{\partial v}{\partial x} = e^x \sin y
\end{align*}

and see that the Cauchy-Riemann equations are fulfilled and conclude that the complex exponential is an analytic function. We can now obtain the complex derivative as

\bee
f'(z) = \frac{\partial u}{\partial x} + \frac{\partial v}{\partial x} = e^x \cos y + j e^x \sin y = e^z \qed
\eee

We can visualize the complex exponential as follows: The function $w = e^z$ takes a complex variable $z$ and transforms it into a complex variable $w$. We can also interpret $z = x+jy$ as a point in the two-dimensional complex $z$-plane with coordinates $(x, y)$ and, similarly, $w = u+jv$ as point in the two-dimensional complex $w$-plane. The function $w = e^z$ is then a mapping from the $z$-plane into the $w$-plane and we can plot the effect of the function on various curves in the $z$-plane.

We start with vertical lines given by $x = a, y = t$ (where $t$ is the real curve parameter). The complex variable $z$ becomes $z =a + jt$ and therefore $w = e^z = e^{x+jt} = e^x e^{jy}$ corresponds to circles with radius $e^x$. The following plot shows the curves in the $z$-plane for different value $a$ on the left, and the corresponding curves in the $w$ plane on the right.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-02-05-complex_01.png}
\end{figure}

The opposite case of horizonal lines ($x = t, y = a$) is shown in the FIgure below. We have $z = t + ja$ and $w = e^z = e^{t + ja} = e^t e^{ja}$ which corresponds to lines with "slope" $e^{ja}$ in the $w$-plane.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-02-05-complex_02.png}
\end{figure}

Finally, "general" lines with different slopes and intersection point are shown below. In this case $z = t + jat$ and $w = e^z = e^{t + jat} = e^t e^{jat}$ corresponds to spirals in the $w$-plane.

\begin{figure}[H]
\centering
\includegraphics[scale=0.4]{images/2026-02-05-complex_04.png}
\end{figure}


\subsection{Trigonometric Functions}

We start with

\begin{align*}
    e^{j \theta} &= \cos \theta + j \sin \theta \\
    e^{-j \theta} &= \cos \theta - j \sin \theta
\end{align*}

Adding the two equations yields

\bee
\cos \theta = \frac{1}{2} \left( e^{j \theta} + e^{-j \theta} \right)
\eee

Subtracting yields

\bee
\sin \theta = \frac{1}{2j} \left( e^{j \theta} - e^{-j \theta} \right)
\eee

This inspires the definition of the trigonometric functions with complex argument as

\be\label{2026-02-05:eq2}
\sin z= \frac{1}{2j} \left( e^{j z} - e^{-j z} \right), \,  \cos z = \frac{1}{2} \left( e^{j z} + e^{-j z} \right)
\ee

We can use these definitions (together with the fact that $e^z$ is analytic), to obtain first derivatives; for example

\begin{align*}
\frac{d \sin z}{dz} &= \frac{d }{dz} \frac{1}{2j} \left( e^{j z} - e^{-j z} \right) = \frac{1}{2j} \left( j e^{j z} + j e^{-j z} \right) \\
&= \frac{1}{2} \left( e^{j z} + e^{-j z} \right) = \cos z \qed
\end{align*}

We can use \eqref{2026-02-05:eq1} to (re)derive some trigonometric identities. We have

\bee
e^{j (z_1 + z_2)} = \cos (z_1 + z_2) + j \sin (z_1 + z_2)
\eee

However, we can also expand the exponential and then split into real and imaginary parts,

\begin{align*}
e^{j (z_1 + z_2)} &= e^{j z_1} \cdot e^{j z_2} = (\cos z_1 + j \sin z_1) \cdot (\cos z_2 + j \sin z_2) \\
&= \cos z_1 \cos z_2 - \sin z_1 \sin z_2 + j \cos z_1 \sin z_2 + j \cos z_2 \sin z_1
\end{align*}

Comparing real and imaginary parts, we obtain

\begin{align*}
    \cos (z_1 + z_2) &= \cos z_1 \cos z_2 - \sin z_1 \sin z_2 \\
    \sin (z_1 + z_2) &= \cos z_1 \sin z_2 + \cos z_2 \sin z_1
\end{align*}

These identities together with \eqref{2026-02-05:eq2} can be used to derive all sorts of interesting results; eq

\begin{align*}
    \sin(z + 2 \pi) &= \cos z \sin 2 \pi + \cos 2 \pi \sin z = \sin z \\
    \cos(-z) &= \frac{1}{2} \left( e^{- j z} + e^{j z} \right) = \cos z \\
    \sin 2z &= \sin (z + z) = \cos z \sin z + \cos z \sin z = 2 \sin z \cos z
\end{align*}


\subsection{Hyperbolic Functions}

They are defined according to

\begin{align*}
    \cosh z &= \frac{1}{2} \left( e^z + e^{-z} \right)
    \sinh z &= \frac{1}{2} \left( e^z - e^{-z} \right)
\end{align*}

We have the following results regarding their periodicity; for the hyperbolic sine

\begin{equation*}
    \sinh (z + 2j\pi k) = \frac{1}{2} \left( e^{z + 2j\pi k} - e^{- z - 2j\pi k} \right) = \frac{1}{2} \left( e^{z} - e^{- z} \right) = \sinh z
\end{equation*}

and for the hyperbolic cosine,

\begin{equation*}
    \cosh (z + 2j\pi k) = \frac{1}{2} \left( e^{z + 2j\pi k} + e^{- z - 2j\pi k} \right) = \frac{1}{2} \left( e^{z} + e^{- z} \right) = \cosh z
\end{equation*}

So the functions are periodic in the imaginary argument. Using purely imaginary arguments shows the close relation between trigonometric and hyperbolic functions as (using \eqref{2026-02-05:eq2})

\begin{align*}
    \sinh jz &= \frac{1}{2} \left( e^{jz} - e^{-jz} \right) = \cos z \\
    \cosh jz &= \frac{1}{2} \left( e^{jz} + e^{-jz} \right) = j \sin z
\end{align*}


By using the definitions from \eqref{2026-02-05:eq2} with an imaginary argument, we obtain

\bee
\sin (j y) = \frac{1}{2j} \left( e^{j \cdot j y} - e^{-j \cdot j y} \right) = \frac{1}{2j} \left( e^{-y} - e^{y} \right) = - \frac{1}{j} \sinh y = j \sinh y
\eee

and

\bee
\cos (j y) = \frac{1}{2} \left( e^{j \cdot j y} + e^{-j \cdot j y} \right) = \frac{1}{2} \left( e^{-y} + e^{y} \right) = \cosh y
\eee

Finally, we can expand the sine for a complex arguments as follows,

\begin{align*}
    \sin(a + jb) &= \cos a \sin jb + \sin a \cos jb \\
    &= j \cos a \sinh b + \sin a \cosh b \\
    &= \sin a \cosh b + j \cos a \sinh b
\end{align*}



\subsection{Complex Logarithm}

For real values, the logarithm is the inverse function of the exponential function. We want to keep this behaviour for complex numbers; although due to the periodicity of $e^z$, things are a bit more complex.

It turns out that we need to be a bit more careful with the argument function $\arg z$: We define this function to take on \emph{any} real value. However, since $e^{j \arg z}$ is periodic with period $2 \pi$, we define a "special" argument function, which takes on values in the interval $[-\pi, \pi]$ only.

\begin{definition}
    We define the \emph{principal value} of $\arg z$, denoted by $\Arg z$, as the unique value of $\arg z$ such that $- \pi < \Arg z \leq \pi$.
\end{definition}

Note that as consequence, $\Arg z$ has discontinuities at $(2k+1) \pi$. As example, consider $e^{j 3 \pi}$. Then $\arg e^{j 3 \pi} = 3 \pi$ and $\Arg e^{j 3 \pi} = \pi$. 

With this definition in place, we can define the complex logarithm as follows.

\begin{definition}
    We define $\Log r = \ln r$ as the natural logarithm of a positive real number $r$. If $z \neq 0$, then we define the complex logarithm $\log z$ as

    \bee
        \log z = \Log |z| + j \arg z = \Log |z| + j \Arg z + 2kj\pi, \quad k = 0, \pm1, \pm 2, \ldots
    \eee
\end{definition}

Proof: If $z \neq 0$, then we can write $z = r e^{j \theta}$ and $|z| = r, \arg z = \theta$. We can develop the complex exponential as

\bee
e^{\log z} = e^{\Log |z| + j \arg z} = e^{\Log |z|} e^{j \arg z} = |z| e^{j \theta} \qed
\eee




%%% Local Variables:
%%% mode: latex
%%% TeX-master: "journal"
%%% End: